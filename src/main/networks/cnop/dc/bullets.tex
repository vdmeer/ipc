\begin{itemize}
    \item A data center network example, here a Spine-Leaf configuration
    \item Tenant VMs to DC servers, then Top of Rack (ToR) and actual fabric
    \item Supports multiple tenants and multiple different applications per tenant
end{itemize}
